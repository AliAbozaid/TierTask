package app.tier.utils

object Constant {
    object SnackBar {
        const val ERROR_DURATION = 4000
    }
    object Network {
        const val DEFAULT_TIMEOUT = 90L
    }

    object Cluster {
        const val KM = 1000.0
    }
}
